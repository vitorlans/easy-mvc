@model IEnumerable<Easy.Models.Comentarios>

@{
    
}

<div class="bs-docs-section">
    <div class="row" style="">
        <div style=" height: 400px; overflow: auto;" class="col-lg-12" >
            <table class="table table-striped table-hover well bs-component">
                <tr>
                    <th>
                    </th>
                    <th>
                    </th>
                    <th></th>
                </tr>

            @foreach (var item in Model) {
                <tr>
                    <td></td>
                    <td>
                        <div class="panel panel-default">
                            <div class="panel-heading"><img style="margin-left: 1%; padding: 2px 2px;" class="btn btn-fab" src="@item.IdUser.Imagem" />&nbsp @item.IdUser.Nome</div>
                            <div class="panel-body">
                                 @Html.DisplayFor(modelItem => item.Comentario)
                            </div>
                        </div>
                        <div class="div-simples">@item.DataComentario</div>
                    </td>

                </tr>
            }

            </table>
        </div>
    </div>
</div>
